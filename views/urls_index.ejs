<!DOCTYPE html>
<html lang="en">
<head>
  <% include ./partials/_header %>
</head>
<body>
  <ul>
    <table cellspacing='16'>
      <% for (let key in urls) { %>
        <tr>
          <td><b><%= key %></b></td>
          <td>→</td>
          <td><%= urls[key].longURL %></td>
          <td>Visits: <%= urls[key].visits.length %> (<%= urls[key].uniqueVisitors.length %> unique)</td>
          <td>Created: <%= formattedDate %></td>
          <td><a href='/urls/<%= key%>'>Edit</a></td>
          <td>
            <form action='<%= `/urls/${key}/delete?_method=DELETE` %>' method='POST' style='display:inline;'>
              <input type='submit' value='Delete'>
            </form>
          </td>
        </tr>
      <%}%>
      <tr>
        <td colspan='5'><a href='/urls/new'>Create a New Short Link</a></td>
      </tr>
    </table>
  </ul>
</body>
</html>